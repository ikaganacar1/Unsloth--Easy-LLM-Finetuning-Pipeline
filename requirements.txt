# Unsloth Fine-tuning Pipeline Requirements

# Core ML Libraries
torch>=2.0.0
torchvision
torchaudio
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.21.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0

# Configuration and Utilities
PyYAML>=6.0
numpy>=1.24.0
pandas>=2.0.0

# Unsloth (install from git)
# Run: pip install unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Optional: Jupyter for dataset processing
jupyter>=1.0.0
ipykernel>=6.0.0